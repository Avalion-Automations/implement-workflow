interface:
  display_name: "Build"
  short_description: "Fast scoped feature delivery with review"
  default_prompt: "Use $basics:build to implement this approved-scope feature quickly on a reviewed integration branch."
