# Expo Run TUI Scope

Change only the new `expo-run` canonical skill and tests, its Codex metadata, required Codex and Claude version manifests, generated Codex/Claude adapter outputs, this Build run's plan/report artifacts, and—only after verified migration—delete `/mnt/coding/Avalion-Solutions/Mias Method/.agents/skills/run-mias-method/`. Do not otherwise change or launch the Mias Method application, change the existing generic `run` skill, launch a tunnel/browser during automated checks, merge or push `devel`, or mutate another Expo project.
