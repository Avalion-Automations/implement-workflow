# Build Request: {{INITIATIVE}}

## Outcome

{{OUTCOME}}

## Documentation lifecycle

- Write initial product/design documents under `docs/draft/`.
- Keep agent process documents under `docs/tracking/`.
- Promote an approved draft by byte-identically copying it to `docs/<topic>.md`, then record semantic version and SHA-256 in `docs/manifest.yaml`.
- Do not begin production implementation until the documented promotion conditions are met.

## Confirmed decisions

{{DECISIONS}}

## Required tracking

Every author creates a goal report before substantive work and a result handoff at completion using `docs/tracking/` templates.
